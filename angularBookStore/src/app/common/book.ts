export class Book {

    sku: string;
    name: string;
    description: string;
    unitPrice: number;
    imageUrl: string;
    active: boolean;
    unitsInStock: number;
    createdOn: Date;
    updatedOn: Date;

    /*
    We will create an array of books with some hard-coded values and display the it in the html table.
    Later we will make a HTTP call to the rest end point and display the data.
    */
}
